# =============================================================================
# Production Environment Configuration
# =============================================================================
# Copy this file to .env.prod and fill in all values:
#   cp env.prod.example .env.prod
#   nano .env.prod
#
# IMPORTANT: Never commit .env.prod to git!
# =============================================================================

# -----------------------------------------------------------------------------
# Project Settings
# -----------------------------------------------------------------------------
PROJECT_NAME=nofollowbot
DOMAIN=nofollowbot.parmenid.tech

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_USER=postgres
# IMPORTANT: Use a strong password! Generate with: openssl rand -base64 24
# If password contains special chars (/, @, %), URL-encode them:
#   / -> %2F
#   @ -> %40
#   % -> %25
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
POSTGRES_DB=mutual_followers

# Full database URL (optional, constructed automatically if not set)
# DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@postgres:5432/mutual_followers

# -----------------------------------------------------------------------------
# Telegram Bot (User-facing)
# -----------------------------------------------------------------------------
# Get token from @BotFather
TELEGRAM_TOKEN=your_telegram_bot_token_here
# Bot username without @
BOT_USERNAME=your_bot_username

# -----------------------------------------------------------------------------
# Telegram Admin Bot (for admin notifications)
# -----------------------------------------------------------------------------
# Can be same as TELEGRAM_TOKEN or a separate admin bot
ADMIN_BOT_TOKEN=your_admin_bot_token_here
# Comma-separated Telegram user IDs who can receive admin notifications
ADMIN_USER_IDS=123456789

# -----------------------------------------------------------------------------
# Instagram
# -----------------------------------------------------------------------------
# Session ID for authenticated Instagram requests
# Get from browser cookies after logging into Instagram
INSTAGRAM_SESSION_ID=your_instagram_session_id_here

# -----------------------------------------------------------------------------
# User Support
# -----------------------------------------------------------------------------
# Telegram username for user support (without @)
MANAGER_USERNAME=issue_resolver

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
UPLOAD_DIR=/app/data/checks
DEBUG=False
MAX_CHECKS_PER_DAY=5

# API URL (internal, for bot to communicate with backend)
API_BASE_URL=http://backend:8000

# -----------------------------------------------------------------------------
# Queue Settings
# -----------------------------------------------------------------------------
MAX_CONCURRENT_CHECKS=1
QUEUE_PROCESSING_INTERVAL=5

# -----------------------------------------------------------------------------
# Referral Program
# -----------------------------------------------------------------------------
REFERRAL_BONUS_CHECKS=1
REFERRAL_REQUIRED_COUNT=10

# -----------------------------------------------------------------------------
# Robokassa Payment Gateway (optional)
# -----------------------------------------------------------------------------
ROBOKASSA_MERCHANT_LOGIN=
ROBOKASSA_PASSWORD_1=
ROBOKASSA_PASSWORD_2=
ROBOKASSA_TEST_MODE=False

# -----------------------------------------------------------------------------
# SSL Certificate (for init-ssl.sh script)
# -----------------------------------------------------------------------------
SSL_EMAIL=admin@parmenid.tech

