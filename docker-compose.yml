services:
  db:
    image: postgres:15-alpine
    container_name: mutual_followers_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-mutual_followers}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: mutual_followers_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mutual_followers_app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
      - ./data/checks:/app/data/checks
      - ./logs:/app/logs
    ports:
      - "8080:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mutual_followers}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - ADMIN_BOT_TOKEN=${ADMIN_BOT_TOKEN:-}
      - MANAGER_USERNAME=${MANAGER_USERNAME:-issue_resolver}
      - INSTAGRAM_SESSION_ID=${INSTAGRAM_SESSION_ID:-}
      - UPLOAD_DIR=/app/data/checks
      - DEBUG=${DEBUG:-True}
      - ADMIN_USER_IDS=${ADMIN_USER_IDS:-}
      - BOT_USERNAME=${BOT_USERNAME:-}
      - MAX_CONCURRENT_CHECKS=${MAX_CONCURRENT_CHECKS:-1}
      - QUEUE_PROCESSING_INTERVAL=${QUEUE_PROCESSING_INTERVAL:-5}
      - REFERRAL_BONUS_CHECKS=${REFERRAL_BONUS_CHECKS:-1}
      - REFERRAL_REQUIRED_COUNT=${REFERRAL_REQUIRED_COUNT:-10}
      - ROBOKASSA_MERCHANT_LOGIN=${ROBOKASSA_MERCHANT_LOGIN:-}
      - ROBOKASSA_PASSWORD_1=${ROBOKASSA_PASSWORD_1:-}
      - ROBOKASSA_PASSWORD_2=${ROBOKASSA_PASSWORD_2:-}
      - ROBOKASSA_TEST_MODE=${ROBOKASSA_TEST_MODE:-True}
      # Session auto-refresh
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=${USE_REDIS:-false}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    depends_on:
      db:
        condition: service_healthy

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mutual_followers_bot
    command: python -m app.bot.main
    volumes:
      - .:/app
      - ./data/checks:/app/data/checks
      - ./logs:/app/logs
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mutual_followers}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - API_BASE_URL=http://app:8000
      - UPLOAD_DIR=/app/data/checks
      - DEBUG=${DEBUG:-True}
      - BOT_USERNAME=${BOT_USERNAME:-}
      - MANAGER_USERNAME=${MANAGER_USERNAME:-issue_resolver}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    depends_on:
      - app
      - db

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mutual_followers_worker
    command: python -m app.services.queue_worker
    volumes:
      - .:/app
      - ./data/checks:/app/data/checks
      - ./logs:/app/logs
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mutual_followers}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - ADMIN_BOT_TOKEN=${ADMIN_BOT_TOKEN:-}
      - ADMIN_USER_IDS=${ADMIN_USER_IDS:-}
      - MANAGER_USERNAME=${MANAGER_USERNAME:-issue_resolver}
      - INSTAGRAM_SESSION_ID=${INSTAGRAM_SESSION_ID:-}
      - UPLOAD_DIR=/app/data/checks
      - DEBUG=${DEBUG:-True}
      - MAX_CONCURRENT_CHECKS=${MAX_CONCURRENT_CHECKS:-1}
      - QUEUE_PROCESSING_INTERVAL=${QUEUE_PROCESSING_INTERVAL:-5}
      # Session auto-refresh
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=${USE_REDIS:-false}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
    depends_on:
      - app
      - db
      - redis
    restart: unless-stopped

  # TaskIQ worker for background session refresh tasks
  taskiq-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mutual_followers_taskiq_worker
    command: taskiq worker app.tasks:broker --fs-discover
    volumes:
      - .:/app
      - ./logs:/app/logs
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mutual_followers}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - ADMIN_BOT_TOKEN=${ADMIN_BOT_TOKEN:-}
      - ADMIN_USER_IDS=${ADMIN_USER_IDS:-}
      - MANAGER_USERNAME=${MANAGER_USERNAME:-issue_resolver}
      - INSTAGRAM_SESSION_ID=${INSTAGRAM_SESSION_ID:-}
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=true
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DEBUG=${DEBUG:-True}
      # Session refresh settings
      - SESSION_REFRESH_DAYS=${SESSION_REFRESH_DAYS:-2}
      - SESSION_MAX_FAIL_COUNT=${SESSION_MAX_FAIL_COUNT:-3}
      - SESSION_PAGE_TIMEOUT=${SESSION_PAGE_TIMEOUT:-30000}
      - SESSION_LOGIN_TIMEOUT=${SESSION_LOGIN_TIMEOUT:-15000}
      - PROACTIVE_CHECK_HOURS=${PROACTIVE_CHECK_HOURS:-6}
      - HEALTH_CHECK_HOURS=${HEALTH_CHECK_HOURS:-1}
    depends_on:
      - db
      - redis
    restart: unless-stopped
    profiles:
      - auto-refresh

  # TaskIQ scheduler for periodic session refresh
  taskiq-scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mutual_followers_taskiq_scheduler
    command: python -m app.tasks.scheduler
    volumes:
      - .:/app
      - ./logs:/app/logs
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-mutual_followers}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - ADMIN_BOT_TOKEN=${ADMIN_BOT_TOKEN:-}
      - ADMIN_USER_IDS=${ADMIN_USER_IDS:-}
      - MANAGER_USERNAME=${MANAGER_USERNAME:-issue_resolver}
      - INSTAGRAM_SESSION_ID=${INSTAGRAM_SESSION_ID:-}
      - REDIS_URL=redis://redis:6379/0
      - USE_REDIS=true
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DEBUG=${DEBUG:-True}
      # Session refresh settings
      - SESSION_REFRESH_DAYS=${SESSION_REFRESH_DAYS:-2}
      - SESSION_MAX_FAIL_COUNT=${SESSION_MAX_FAIL_COUNT:-3}
      - SESSION_PAGE_TIMEOUT=${SESSION_PAGE_TIMEOUT:-30000}
      - SESSION_LOGIN_TIMEOUT=${SESSION_LOGIN_TIMEOUT:-15000}
      - PROACTIVE_CHECK_HOURS=${PROACTIVE_CHECK_HOURS:-6}
      - HEALTH_CHECK_HOURS=${HEALTH_CHECK_HOURS:-1}
    depends_on:
      - db
      - redis
      - taskiq-worker
    restart: unless-stopped
    profiles:
      - auto-refresh

volumes:
  postgres_data:
  redis_data:
